import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";
import answerApi from "../api/answerApi";

const initialState = {

}

export const answerManagementSlice = createSlice({
    name: 'answerManagement',
    initialState,
    reducers: {

    },
    extraReducers: (builder) => {
        
    }
})